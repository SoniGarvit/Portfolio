import { Container } from "@mui/material";
import Hero from "../components/Hero";

const Home = () => {
  return (
    <Container sx={{ py: 8 }}>
      <Hero />
    </Container>
  );
};

export default Home;
